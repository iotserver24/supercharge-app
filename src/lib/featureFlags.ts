/**
 * Compile-time surface switches. A disabled surface keeps its implementation;
 * only the user-facing entry points are hidden. Flip a flag to true to expose
 * the surface again.
 */
export const REMOTE_CONTROL_ENABLED = false;
