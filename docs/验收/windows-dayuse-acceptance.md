# Windows day-use acceptance (0.1.9+)

1. Install from GitHub Release (`setup.exe` or portable zip). SmartScreen may warn (unsigned).
2. First run: Doctor finds/installs Grok Build CLI; configure official login or provider.
3. Create a project (path with spaces OK) — **no cmd console flash storm**.
4. One tool-using chat turn; paste screenshot once — **single attachment**.
5. Settings → About → Check for updates (download installer if newer).
6. Optional: Remote control → Phone mirror starts **read-only**; regenerate link works.
